# codex

Codex CLI config, linked by each OS restore script — `~/.config/codex` on Linux, `~/.codex` on macOS/Windows.

- `AGENTS.md` — global instructions
- `config.toml` — main config
- `agents/`, `skills/` — GSD (get-shit-done) agents and skills
