# opencode

OpenCode config, linked to `~/.config/opencode` by each OS restore script.

- `opencode.json`, `AGENTS.md` — main config + global instructions
- `agents/`, `command/` — GSD (get-shit-done) agents and slash commands
- `get-shit-done/`, `hooks/`, `plugins/` — GSD plugin and supporting state
