# gemini

Gemini CLI / Antigravity config, linked to `~/.gemini` by each OS restore script.

- `config/` — Gemini CLI settings (`GEMINI.md` global instructions, `mcp_config.json`)
- `antigravity/`, `antigravity-ide/` — Antigravity editor config (`antigravity-backup/` is a local snapshot)
