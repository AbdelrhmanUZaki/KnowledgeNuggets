# agents

Skills for AI coding agents, linked to `~/.agents` by each OS restore script.

`skills/` holds the caveman family — ultra-compressed communication/commit/review/compress modes — plus `compress` for shrinking memory files. See each skill's `SKILL.md` for usage.
